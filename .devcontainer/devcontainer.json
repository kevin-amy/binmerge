{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "forwardPorts": [],
  "remoteUser": "codespace",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.makefile-tools",
        "ms-vscode.cmake-tools"
      ]
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y build-essential gcc g++ make mingw-w64 mingw-w64-tools && which x86_64-w64-mingw32-gcc",
  "postStartCommand": "echo 'BinMerger compilation environment ready. Run: make -f Makefile.linux' || true"
}